import {StyleSheet} from 'react-native';

export const filtersStyles = StyleSheet.create({
  container: {
    gap: 5,
    paddingHorizontal: 15,
  },
  header: {
    fontSize: 18,
    fontWeight: 'bold',
  },
  subHeader: {
    fontSize: 16,
    fontWeight: 'medium',
  },
});
